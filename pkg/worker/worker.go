// Package worker holds worker-side product logic (actor host, package store, overlay member).
//
// Skeleton only. Live code must not import orvalho/attic.
package worker

// Version is the worker package version string.
const Version = "0.0.0-dev"

// Role is the SPEC role name for this package.
const Role = "worker"
